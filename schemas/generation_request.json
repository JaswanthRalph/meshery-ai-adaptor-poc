{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AI Provider Generation Request",
  "description": "Request payload for NL→Design generation via Meshery AI providers",
  "type": "object",
  "required": ["prompt", "connection_id"],
  "properties": {
    "prompt": {
      "type": "string",
      "description": "Natural language description of the desired infrastructure",
      "minLength": 1,
      "maxLength": 10000,
      "examples": [
        "Deploy a 3-replica nginx web server with a LoadBalancer service",
        "Create a Redis cluster with persistence and a horizontal pod autoscaler"
      ]
    },
    "connection_id": {
      "type": "string",
      "format": "uuid",
      "description": "ID of the AI provider connection to use"
    },
    "user_id": {
      "type": "string",
      "description": "User ID (populated by auth middleware)"
    }
  },
  "additionalProperties": false
}
